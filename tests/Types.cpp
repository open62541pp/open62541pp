#include <string>
#include <vector>

#include "catch2/catch.hpp"
#include "open62541pp/Types.h"

using namespace opcua;

TEST_CASE("getType", "[opcua]") {
    // REQUIRE(true);
}

TEST_CASE("OP CUA getVectorType", "[opcua]") {
    // REQUIRE(getVectorType<std::vector<float>>() == Type::Float);
}